// all these pages will want to have consistent layout.
// layout is essentially a wrapper which just a head, header and footer.
// thus we can define a layout here and reuse it in all of these different pages
export default function Layout() {
  return <div>asdf</div>;
}
